apiVersion: v1
kind: Service
metadata:
  name: jenkins-volume
spec:
  type: LoadBalancer
  ports:
    - port: 8080
      targetPort: 8080
      name: jenkins-port-volume
  selector:
    app: jenkins-volume
